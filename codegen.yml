overwrite: true

schema: "https://api-sa-east-1.hygraph.com/v2/clasxiryf0gb601usdivp156b/master"

documents: "./src/graphql/**/*.graphql"

generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
    config:
      withHooks: true

hooks:
  afterAllFileWrite:
    - eslint --fix
